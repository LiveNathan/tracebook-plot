{
  "name": "tracebook-plot",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "tailwind:watch": "npx tailwindcss -i ./style.css -o ./output.css --watch",
    "start": "concurrently \"npm run tailwind:watch\" \"npm run dev\"",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^7.0.0",
    "tailwindcss": "^3.4.4",
    "vite": "^5.2.0"
  }
}